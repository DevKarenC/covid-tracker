import React from 'react';
import '../css-loader.css';

const Loading = () => {
  return (
    <div className="loader loader-default is-active" data-text="Loading..." />
  );
};

export default Loading;
